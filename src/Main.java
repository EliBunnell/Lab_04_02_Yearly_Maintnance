public class Main {
    public static void main(String[] args) {

        double springCost = 10000;
        double summerCost = 8000;
        double fallCost = 6000;
        double winterCost = 6000;
        double annualCost = springCost + summerCost + fallCost + winterCost;

        System.out.println("The cost for Spring is " + springCost + " and the cost for summer is " + summerCost + " and the cost for fall is " + fallCost + " and the winter cost is " + winterCost + " and the cost for annual is " + annualCost );
    }
}
